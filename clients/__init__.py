"""
MCP-only client architecture organized by data type.
Provides clean separation between data access (MCP), formatting (adapters), 
LLM interactions (OpenRouter), and user interfaces (CLI clients).
"""